/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {
        appIsrStatus: false,
        buildActivity: false,
        // buildActivityPosition: false, // optional
    },
};
export default nextConfig;
